# tokens

A `token` is a reference entry for a blockchain asset (name, chain, standard, address). The module provides a
catalogue of such tokens, including a seeder that populates standard Ethereum assets, for other modules to reference.

## Entry point

`api.ITokenService` — `OPEN` module (`api` is marked `ApplicationModule.Type.OPEN`).
